name: Build Android APK

on:
  push:
    # L'utilisation de '**' permet de déclencher le workflow sur n'importe quelle branche
    branches:
      - '**'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Create .env file
        run: |
          cat > .env << EOF
          VITE_GEMINI_API_KEY=${{ secrets.VITE_GEMINI_API_KEY }}
          VITE_PERSONAL_SUPABASE_ANON_KEY=${{ secrets.VITE_PERSONAL_SUPABASE_ANON_KEY }}
          VITE_PERSONAL_SUPABASE_URL=${{ secrets.VITE_PERSONAL_SUPABASE_URL }}
          VITE_SUPABASE_PROJECT_ID=${{ secrets.VITE_SUPABASE_PROJECT_ID }}
          VITE_SUPABASE_PUBLISHABLE_KEY=${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}
          EOF

      - name: Install dependencies
        run: |
          npm install
          npm install @capacitor/android@^8.0.0 @capacitor/core@^8.0.0 @capgo/capacitor-health@^8.4.2


      - name: Build web app
        run: npm run build

      - name: Verify dist folder
        run: |
          ls -la dist/
          cat dist/index.html | head -20

      - name: Clean and Sync Capacitor
        run: |
          # Nettoyage complet assets
          rm -rf android/app/src/main/assets/public/*
          # Vérifier que dist/index.html existe
          ls -la dist/index.html
          # Copier les assets web
          npx cap copy android
          # Vérifier la copie
          ls -la android/app/src/main/assets/public/
          # Sync complet
          npx cap sync android

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Generate Gradle Wrapper
        run: gradle wrapper --gradle-version 8.13
        working-directory: android

        
      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14
